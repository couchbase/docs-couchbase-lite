
ext {
    CBL_VERSION='3.2.0-SNAPSHOT'
    EXT_VERSION='1.0.0-18'
}

buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.0.2'
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
