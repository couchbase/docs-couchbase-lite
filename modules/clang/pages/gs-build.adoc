= Build and Run
:page-aliases: start/clang-gs-build.adoc
:page-edition: 3.0
:description: Build and run a starter app to validate your install of Couchbase Lite on C
:keywords: mobile edge nosql api iOS C web-app device-app

include::partial$_std-cbl-hdr-clang.adoc[]

include::{root-partials}block-abstract.adoc[]

== Verify Install

:param-page: {page-relative-src-path}
include::{root-partials}_get-started-topic-group.adoc[]


Now you have the Couchbase Lite for C installed, you can verify it builds correctly.

Enter the sample C app below into your editor of choice and build it.

Running it will create a database, add a document, retrieve the document, update the document and then delete the document.

[#ex-clang-starter]
[source, c]
----
//
//  main.c
//  clang300testProd
//
//  Created by Ian Bridge on 06/08/2021.
//

// STILL A WORK IN PROGRESS


#include <stdio.h>
#include "cbl/CBLDatabase.h"
#include "cbl/CBLDocument.h"
#include "cbl/CBLQuery.h"
//#include "cbl/CBLMutableDocument.h"
#include "fleece/Fleece+CoreFoundation.h"

int main(int argc, const char * argv[]) {
    // insert code here...
    printf("Hello from Couchbase Lite for C!\n");

    const char* dbname = "mydb";

    CBLDatabaseConfiguration* dbcfg;

    CBLError* err;
    CBLDatabase* db = CBLDatabase_Open(FLStr(dbname),dbcfg, err);

    CBLDocument* doc = CBLDocument_CreateWithID(FLStr("1000"));

    FLMutableDict* body = FLMutableDict_New();
    FLSlot* slot = FLMutableDict_Set(*body, FLStr("type"));
    FLSlot_SetString(* slot, FLStr("pets"));

    CBLDocument_SetProperties(doc, "type:pet" );

    return 0;
}
///Users/ianbridge/CouchbaseDev/test-clang/test-clang-proj/clang-test-prod/clang-test-prod
///test-clang-proj/clang-test-prod/clang-test-prod

----



include::{root-partials}block-related-content-start.adoc[]