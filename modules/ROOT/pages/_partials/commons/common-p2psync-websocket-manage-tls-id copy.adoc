// common-p2psync-websocket-manage-tls-id.adoc
// Managing TLS Identities for P2P Sync over Websockets
//

// BEGIN CONTENT

== Overview

This describes the configuration and management of TLS identities

== Creating TLS Identity

=== Anonymous Cert
Self signed certificate is generated by Couchbase Lite

 listenerConfig.disableTLS  = false // Use with anonymous self signed cert
  listenerConfig.tlsIdentity = nil

=== Importing a cert

Import certificate that is can be bundled with the app or it can be in keychain

// This code snippet is related to the first option when certificate is bundled
 if let tlsIdentity = self.getTLSIdentityFromPKCS12DataWithCertLabel(kListenerCertLabel) {
                    listenerConfig.disableTLS  = false // Use with anonymous self signed cert
                    listenerConfig.tlsIdentity = tlsIdentity
                }
== Deleting TLS Identity
== Convenience API Components

include::{root-partials}p2p-api-tlsid-{param-platform}.adoc[tag=components]

== Get TLS Identity with Label

include::{root-partials}p2p-api-tlsid-{param-platform}.adoc[tag=method-get-with-label]

== Get TLS Identity with SecIdentity object

include::{root-partials}p2p-api-tlsid-{param-platform}.adoc[tag=method-get-with-cert]

== Create TLS Identity

include::{root-partials}p2p-api-tlsid-{param-platform}.adoc[tag=method-create]

== Delete TLS Identity

include::{root-partials}p2p-api-tlsid-{param-platform}.adoc[tag=method-delete]

== Import TLS Identity

include::{root-partials}p2p-api-tlsid-{param-platform}.adoc[tag=method-import]

== Bundling Certs with Your App

An approach use-case tbd


// END CONTENT